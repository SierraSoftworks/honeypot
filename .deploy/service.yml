---
apiVersion: v1
kind: Service
metadata:
  name: honeypot-server
spec:
  selector:
    app.kubernetes.io/name: honeypot
    app.kubernetes.io/instance: honeypot-server
  ports:
    - name: ssh
      port: 22
      targetPort: ssh
      protocol: TCP
    - name: http
      port: 80
      targetPort: http
      protocol: TCP